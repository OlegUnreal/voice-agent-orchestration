apiVersion: v1
kind: Secret
metadata:
  name: helix-secrets
  namespace: helix
type: Opaque
stringData:
  # Optional: xAI Grok API key for narration
  # HELIX_XAI_API_KEY: "your-xai-api-key"
  # Optional: xAI API key for voice UI
  # XAI_API_KEY: "your-xai-api-key"
  # Placeholder — replace with actual secrets or use external secret management
  placeholder: "replace-with-real-secrets"
