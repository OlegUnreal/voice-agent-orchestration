apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helix-data-pvc
  namespace: helix
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  # storageClassName: standard  # Uncomment and adjust for your cluster
