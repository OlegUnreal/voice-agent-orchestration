# Point Helix at *your* Streamable HTTP MCP server.
# Write tools (orders, cancel, kill-switch) are discovered and never auto-run.

HELIX_MCP_URL=https://mcp.example.ts.net/mcp
HELIX_MCP_PROTOCOL=2025-06-18
HELIX_MCP_TIMEOUT=8
# HELIX_MCP_TOKEN=
HELIX_ALLOW_SIMULATOR=true
